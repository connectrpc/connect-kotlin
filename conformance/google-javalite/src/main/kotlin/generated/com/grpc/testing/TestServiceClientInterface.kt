// Copyright 2022-2023 Buf Technologies, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by connect-kotlin. DO NOT EDIT.
//
// Source: grpc/testing/test.proto
//
package com.grpc.testing

import build.buf.connect.BidirectionalStreamInterface
import build.buf.connect.ClientOnlyStreamInterface
import build.buf.connect.Headers
import build.buf.connect.ResponseMessage
import build.buf.connect.ServerOnlyStreamInterface

/**
 *  A simple service to test the various types of RPCs and experiment with
 *  performance with various types of payload.
 */
public interface TestServiceClientInterface {
  /**
   *  One empty request followed by one empty response.
   */
  public suspend fun emptyCall(request: Empty, headers: Headers = emptyMap()):
      ResponseMessage<Empty>

  /**
   *  One request followed by one response.
   */
  public suspend fun unaryCall(request: SimpleRequest, headers: Headers = emptyMap()):
      ResponseMessage<SimpleResponse>

  /**
   *  One request followed by one response. This RPC always fails.
   */
  public suspend fun failUnaryCall(request: SimpleRequest, headers: Headers = emptyMap()):
      ResponseMessage<SimpleResponse>

  /**
   *  One request followed by one response. Response has cache control
   *  headers set such that a caching HTTP proxy (such as GFE) can
   *  satisfy subsequent requests.
   */
  public suspend fun cacheableUnaryCall(request: SimpleRequest, headers: Headers = emptyMap()):
      ResponseMessage<SimpleResponse>

  /**
   *  One request followed by a sequence of responses (streamed download).
   *  The server returns the payload with client desired type and sizes.
   */
  public suspend fun streamingOutputCall(headers: Headers = emptyMap()):
      ServerOnlyStreamInterface<StreamingOutputCallRequest, StreamingOutputCallResponse>

  /**
   *  One request followed by a sequence of responses (streamed download).
   *  The server returns the payload with client desired type and sizes.
   *  This RPC always responds with an error status.
   */
  public suspend fun failStreamingOutputCall(headers: Headers = emptyMap()):
      ServerOnlyStreamInterface<StreamingOutputCallRequest, StreamingOutputCallResponse>

  /**
   *  A sequence of requests followed by one response (streamed upload).
   *  The server returns the aggregated size of client payload as the result.
   */
  public suspend fun streamingInputCall(headers: Headers = emptyMap()):
      ClientOnlyStreamInterface<StreamingInputCallRequest, StreamingInputCallResponse>

  /**
   *  A sequence of requests with each request served by the server immediately.
   *  As one request could lead to multiple responses, this interface
   *  demonstrates the idea of full duplexing.
   */
  public suspend fun fullDuplexCall(headers: Headers = emptyMap()):
      BidirectionalStreamInterface<StreamingOutputCallRequest, StreamingOutputCallResponse>

  /**
   *  A sequence of requests followed by a sequence of responses.
   *  The server buffers all the client requests and then serves them in order. A
   *  stream of responses are returned to the client when the server starts with
   *  first request.
   */
  public suspend fun halfDuplexCall(headers: Headers = emptyMap()):
      BidirectionalStreamInterface<StreamingOutputCallRequest, StreamingOutputCallResponse>

  /**
   *  The test server will not implement this method. It will be used
   *  to test the behavior when clients call unimplemented methods.
   */
  public suspend fun unimplementedCall(request: Empty, headers: Headers = emptyMap()):
      ResponseMessage<Empty>

  /**
   *  The test server will not implement this method. It will be used
   *  to test the behavior when clients call unimplemented streaming output methods.
   */
  public suspend fun unimplementedStreamingOutputCall(headers: Headers = emptyMap()):
      ServerOnlyStreamInterface<Empty, Empty>
}
